# localci-workflow-classification: retired
name: lockfile-check

on:
  workflow_dispatch:

jobs:
  retired-lockfile-check:
    name: Retired - Lockfile Check
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: |
          echo "This workflow was retired by LOCALCI."
          echo "Use make agent-fast or make agent-gate instead of a separate hosted lockfile gate."
