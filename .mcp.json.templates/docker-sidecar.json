{
  "mcpServers": {
    "code-index-sidecar": {
      "command": "docker",
      "args": [
        "exec", 
        "-i", 
        "mcp-sidecar",
        "python",
        "-m",
        "mcp_server.cli",
        "stdio"
      ],
      "env": {
        "MCP_WORKSPACE_ROOT": "/workspace",
        "VOYAGE_API_KEY": "${VOYAGE_API_KEY:-}",
        "MCP_LOG_LEVEL": "${MCP_LOG_LEVEL:-INFO}"
      }
    }
  }
}