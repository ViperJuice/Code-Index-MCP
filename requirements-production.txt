# Production Requirements for MCP Server
# This file contains production-optimized dependencies with specific versions
# for stability and security in production deployments.

# Core MCP Protocol (Required)
mcp==1.0.0                       # Model Context Protocol implementation

# Web Framework & Server (Required)
fastapi==0.115.6                 # FastAPI web framework
uvicorn[standard]==0.32.1        # ASGI server with extra features
gunicorn==23.0.0                  # Production WSGI server

# Database & Storage (Required)
sqlite3                          # SQLite database (system package)
aiosqlite==0.20.0                # Async SQLite driver

# Language Processing & Tree-sitter (Required)
tree-sitter==0.24.0              # Parser generator framework
tree-sitter-languages==1.10.2    # All language grammars bundle
jedi==0.19.2                     # Python static analysis

# Individual tree-sitter language parsers (Required for new language support)
tree-sitter-c-sharp==0.20.0      # C# parser
tree-sitter-bash==0.20.0         # Bash parser
tree-sitter-haskell==0.14.0      # Haskell parser
tree-sitter-scala==0.19.0        # Scala parser
tree-sitter-lua==0.0.14          # Lua parser
tree-sitter-yaml==0.5.0          # YAML parser
tree-sitter-toml==0.5.1          # TOML parser
tree-sitter-json==0.19.0         # JSON parser
tree-sitter-markdown==0.7.1      # Markdown parser
tree-sitter-csv==0.1.1           # CSV parser

# Data Validation & Serialization (Required)
pydantic==2.11.2                 # Data validation and serialization
pydantic-settings==2.8.0         # Settings management

# HTTP Client & File Operations (Required)
httpx==0.28.1                    # Modern async HTTP client
aiofiles==24.1.0                 # Async file operations

# Configuration & Environment (Required)
python-dotenv==1.0.1             # Environment variable loading
click==8.2.1                     # CLI framework

# Performance & Optimization (Required)
orjson==3.10.13                  # Fast JSON serialization

# Core Utilities (Required)
typing-extensions==4.13.2        # Extended typing support

# Production Logging & Monitoring (Required)
structlog==24.4.0                # Structured logging
prometheus-client==0.21.1        # Prometheus metrics
rich==14.0.0                     # Rich text formatting for logs

# Repository Management Dependencies (Required for new features)
watchdog==6.0.0                  # File system monitoring
gitpython==3.1.43                # Git repository interaction

# Testing (Production Health Checks)
pytest==8.3.4                    # Testing framework for health checks
pytest-asyncio==0.25.0           # Async testing support

# Optional: Redis Caching (Uncomment if using Redis)
# redis[hiredis]==6.2.0           # Redis client with C extensions
# aioredis==2.0.1                 # Async Redis client

# Optional: PostgreSQL Support (Uncomment if using PostgreSQL)
# asyncpg==0.29.0                 # PostgreSQL async driver  
# sqlalchemy[asyncio]==2.0.35     # ORM with async support
# alembic==1.14.0                 # Database migrations

# Optional: Advanced Security (Uncomment if needed)
# PyJWT==2.10.1                   # JWT tokens
# passlib[bcrypt]==1.7.4          # Password hashing
# python-multipart==0.0.20        # Form data parsing
# cryptography==44.0.0            # Additional crypto functions

# Optional: Semantic Search (Uncomment if using vector embeddings)
# sentence-transformers==3.2.1    # Transformer models for embeddings
# qdrant-client==1.9.1            # Vector database client
# numpy==2.2.1                    # Numerical computing

# Optional: Task Queue (Uncomment if using background tasks)
# celery[redis]==5.4.0            # Distributed task queue
# flower==2.0.1                   # Celery monitoring

# Optional: Advanced Monitoring (Uncomment for production observability)
# sentry-sdk[fastapi]==2.19.2     # Error tracking
# opentelemetry-api==1.29.0       # OpenTelemetry instrumentation
# opentelemetry-sdk==1.29.0       # OpenTelemetry SDK